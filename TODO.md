# TODO - Replace

- [x] Criar tela de reservas (`Front-End/src/pages/Reservas.jsx` e `Front-End/src/pages/Reservas.css`).
- [x] Persistir reservas no `localStorage` com chave `reservas`.
- [x] Atualizar `/reservas` na navegação (navbar em Home/Produtos/About/Cadastro).
- [x] Criar rota em `Front-End/src/App.jsx` para `/reservas`.
- [x] Adicionar botão **Reservar** nos cards de produtos em `Front-End/src/pages/Produtos.jsx`.
- [x] Garantir que não existam reservas duplicadas do mesmo produto (por `id`).
- [x] Cancelar reserva removendo do `localStorage` e atualizando UI + mensagem "Reserva cancelada com sucesso".
- [x] Verificar que compila sem erros (`cd Front-End` + `npm run build`).
